import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  data: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 16,
    lineHeight: 22,
  },
  title: { textAlign: 'center' },
});
