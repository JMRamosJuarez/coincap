import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    overflow: 'hidden',
  },
  wrap: {
    borderRadius: 2,
    overflow: 'hidden',
  },
  skeleton: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },
});
